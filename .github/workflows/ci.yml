name: CI

on: [push, pull_request]

jobs:
  hemtt:
    name: HEMTT
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v2

    - name: Build with HEMTT
      id: build
      run: |
        ./hemtt build